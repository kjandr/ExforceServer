<!DOCTYPE html>
<html lang="de">
<head>
    <style>
        body {
            background-color: #1e1e1e;
            color: #eee;
            font-family: Arial, sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
        }
        .dark-mode {
            background-color: #1e1e1e;
            color: #eee;
            font-family: Arial, sans-serif;
            min-height: 100vh;
            padding: 20px;
        }

        .dark-mode h2 {
            text-align: center;
            color: #fff;
            margin-bottom: 1.5rem;
        }

        .password-form {
            max-width: 400px;
            margin: auto;
            background-color: #2a2a2a;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 12px rgba(0, 0, 0, 0.3);
        }

        .password-form label {
            display: block;
            font-weight: bold;
            margin-bottom: 0.5rem;
        }

        .password-form input {
            width: 100%;
            padding: 10px;
            margin-bottom: 1.5rem;
            border: 1px solid #555;
            background-color: #333;
            color: white;
            border-radius: 4px;
        }

        .form-actions {
            display: flex;
            justify-content: space-between;
            gap: 10px;
        }

        .btn-success,
        .btn-info {
            border: none;
            color: white;
            padding: 10px 18px;
            border-radius: 4px;
            cursor: pointer;
            width: 48%;
        }

        .btn-success {
            background-color: #28a745;
        }

        .btn-info {
            background-color: #007bff;
        }

        .btn-success:hover {
            background-color: #218838;
        }

        .btn-info:hover {
            background-color: #0069d9;
        }

        a button {
            all: unset;
            display: block;
            text-align: center;
            width: 100%;
            cursor: pointer;
        }
    </style>
</head>

<body>
    <div class="dark-mode">
        <h2 style="text-align:center; margin: 2rem 0;">🔑 Passwort ändern für Benutzer ID: <%= id %></h2>

        <form method="POST" action="/admin/user/change-password/<%= id %>" class="password-form">
            <label for="newPassword">Neues Passwort:</label>
            <input type="password" name="newPassword" id="newPassword" required placeholder="Neues Passwort eingeben">

            <div class="form-actions">
                <button type="submit" class="btn-success">✅ Passwort speichern</button>
                <a href="/admin/user/list"><button type="button" class="btn-info">⬅️ Zurück</button></a>
            </div>
        </form>
    </div>
</body>
</html>
